import type { HolderSummary,MarketData,Risk,RiskDeduction,Token } from "@/types/token";
export function calculateRisk(token:Token,market:MarketData|null,holders:HolderSummary|null):Risk{const d:RiskDeduction[]=[];const add=(label:string,points:number,detail:string)=>d.push({label,points,detail});const top=holders?.topHolderPercentage;if(top!=null){if(top>30)add("Largest holder concentration",25,`Largest holder owns ${top.toFixed(1)}%`);else if(top>20)add("Largest holder concentration",15,`Largest holder owns ${top.toFixed(1)}%`);else if(top>10)add("Largest holder concentration",5,`Largest holder owns ${top.toFixed(1)}%`)}const ten=holders?.top10Percentage;if(ten!=null){if(ten>70)add("Top 10 concentration",20,`Top 10 hold ${ten.toFixed(1)}%`);else if(ten>50)add("Top 10 concentration",10,`Top 10 hold ${ten.toFixed(1)}%`)}if(market?.liquidityUsd!=null&&market.liquidityUsd<10000)add("Low liquidity",15,"USD liquidity is below $10K");if(token.name==="Unknown Token"||token.symbol==="UNKNOWN")add("Contract metadata",12,"Standard token metadata is incomplete");if(market&&market.buys24h!=null&&market.sells24h!=null){const total=market.buys24h+market.sells24h;if(total>20&&Math.min(market.buys24h,market.sells24h)/total<.05)add("Trading imbalance",5,"24h buys and sells are extremely imbalanced")}const score=Math.max(0,100-d.reduce((n,x)=>n+x.points,0));return {score,level:score>=80?"Lower Risk Indicators":score>=60?"Moderate Risk Indicators":score>=40?"High Risk Indicators":"Very High Risk Indicators",deductions:d,calculatedAt:new Date().toISOString()}}
