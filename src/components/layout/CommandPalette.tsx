"use client";
import {useEffect,useMemo,useRef,useState} from "react";import {useRouter} from "next/navigation";import {Search,TrendingUp,Sparkles,Star,GitCompareArrows,Clock,ArrowRight} from "lucide-react";import {isAddress,getAddress} from "viem";import type {RankedToken} from "@/types/token";import {useAppState} from "@/components/providers/AppState";import {shortenAddress} from "@/lib/utils/format";
type Result={label:string;detail:string;href:string;icon:React.ReactNode};
export function CommandPalette({open,onClose}:{open:boolean;onClose():void}){const[q,setQ]=useState(""),[remote,setRemote]=useState<RankedToken[]>([]),[active,setActive]=useState(0),router=useRouter(),input=useRef<HTMLInputElement>(null),state=useAppState();useEffect(()=>{if(open){setActive(0);setTimeout(()=>input.current?.focus(),20)}else setQ("")},[open]);useEffect(()=>{if(q.trim().length<2||isAddress(q.trim())){setRemote([]);return}const c=new AbortController(),timer=setTimeout(()=>fetch(`/api/search?q=${encodeURIComponent(q)}`,{signal:c.signal}).then(r=>r.json()).then(x=>setRemote(x.data??[])).catch(()=>{}),220);return()=>{clearTimeout(timer);c.abort()}},[q]);const results=useMemo<Result[]>(()=>{const address=isAddress(q.trim())?getAddress(q.trim()):null;if(address)return[{label:`Open ${shortenAddress(address)}`,detail:"Valid contract address",href:`/token/${address}`,icon:<ArrowRight/>}];if(q.trim())return remote.map(x=>({label:`${x.token.symbol} · ${x.token.name}`,detail:shortenAddress(x.token.address),href:`/token/${x.token.address}`,icon:<Search/>}));return[...state.recent.slice(0,4).map(x=>({label:x.symbol,detail:`Recently viewed · ${x.name}`,href:`/token/${x.address}`,icon:<Clock/>})),{label:"View Trending",detail:"Tokens gaining tracked activity",href:"/discover?view=trending",icon:<TrendingUp/>},{label:"View New Tokens",detail:"Recently indexed by HoodLens",href:"/discover?view=new",icon:<Sparkles/>},{label:"Open Watchlist",detail:"Your locally saved tokens",href:"/watchlist",icon:<Star/>},{label:"Compare Tokens",detail:"Compare 2–4 tokens",href:"/compare",icon:<GitCompareArrows/>}]},[q,remote,state.recent]);function go(href:string){router.push(href);onClose()}if(!open)return null;return <div className="palette-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="palette" role="dialog" aria-modal="true" aria-label="Search HoodLens" onKeyDown={e=>{if(e.key==="Escape")onClose();if(e.key==="ArrowDown"){e.preventDefault();setActive(x=>Math.min(x+1,results.length-1))}if(e.key==="ArrowUp"){e.preventDefault();setActive(x=>Math.max(x-1,0))}if(e.key==="Enter"&&results[active])go(results[active].href)}}><div className="palette-input"><Search/><input ref={input} value={q} onChange={e=>{setQ(e.target.value);setActive(0)}} placeholder="Search HoodLens…" aria-label="Search tokens and commands"/><kbd>ESC</kbd></div><div className="palette-results">{results.length?results.map((r,i)=><button key={r.href} className={i===active?"selected":""} onMouseEnter={()=>setActive(i)} onClick={()=>go(r.href)}><span>{r.icon}</span><div><strong>{r.label}</strong><small>{r.detail}</small></div></button>):<div className="empty-compact"><strong>No indexed token found</strong><span>Try a full contract address.</span></div>}</div><div className="palette-foot"><span>↑↓ navigate</span><span>↵ open</span></div></div></div>}
