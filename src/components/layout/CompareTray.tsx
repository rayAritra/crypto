"use client";import Link from "next/link";import {X} from "lucide-react";import {useAppState} from "@/components/providers/AppState";export function CompareTray(){const s=useAppState();if(!s.ready||!s.compare.length)return null;return <aside className="compare-tray"><strong>Compare</strong><div>{s.compare.map(x=><span key={x.address}>{x.symbol}<button onClick={()=>s.removeCompare(x.address)} aria-label={`Remove ${x.symbol}`}><X size={13}/></button></span>)}</div><Link className={`button primary ${s.compare.length<2?"disabled":""}`} aria-disabled={s.compare.length<2} href={s.compare.length>=2?`/compare?tokens=${s.compare.map(x=>x.address).join(",")}`:"/compare"}>Compare {s.compare.length} Tokens</Link></aside>}
