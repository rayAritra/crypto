"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {usePathname,useSearchParams} from "next/navigation";import {Menu,Search,ScanSearch,X} from "lucide-react";import {CommandPalette} from "./CommandPalette";
const links=[["/discover","Discover"],["/discover?view=trending","Trending"],["/discover?view=new","New Tokens"],["/watchlist","Watchlist"],["/compare","Compare"]];export function Navbar(){const[palette,setPalette]=useState(false),[menu,setMenu]=useState(false),path=usePathname(),params=useSearchParams(),view=params.get("view");function current(href:string){const[targetPath,query]=href.split("?");if(path!==targetPath)return false;if(targetPath!=="/discover")return true;const targetView=new URLSearchParams(query??"").get("view");return targetView?view===targetView:view!=="trending"&&view!=="new"}useEffect(()=>{const key=(e:KeyboardEvent)=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setPalette(true)}};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[]);return <><header className="site-header"><nav className="shell nav"><Link href="/" className="brand"><span className="brand-mark"><ScanSearch size={18}/></span>HoodLens</Link><div className={`nav-links ${menu?"open":""}`}>{links.map(([href,label])=><Link key={href} href={href} className={current(href)?"current":""} onClick={()=>setMenu(false)}>{label}</Link>)}</div><div className="nav-actions"><span className="network"><i/>Robinhood Chain</span><button className="search-trigger" onClick={()=>setPalette(true)}><Search size={15}/><span>Search</span><kbd>⌘ K</kbd></button><button className="mobile-menu" onClick={()=>setMenu(x=>!x)} aria-label="Toggle navigation">{menu?<X/>:<Menu/>}</button></div></nav></header><CommandPalette open={palette} onClose={()=>setPalette(false)}/></>}
