"use client";import {useState} from "react";import {Copy,Check} from "lucide-react";import {notify} from "./ToastViewport";
export function CopyButton({value}:{value:string}){const[done,setDone]=useState(false);return <button className="copy-button" aria-label="Copy contract address" onClick={async()=>{await navigator.clipboard.writeText(value);setDone(true);notify("Contract copied");setTimeout(()=>setDone(false),1500)}}>{done?<Check size={15}/>:<Copy size={15}/>}</button>}
