"use client";import {Star} from "lucide-react";import type {RankedToken,Token} from "@/types/token";import {useAppState} from "@/components/providers/AppState";import {notify} from "./ToastViewport";
export function WatchButton({item,compact=false}:{item:RankedToken|{token:Token;metrics:RankedToken["metrics"]};compact?:boolean}){const state=useAppState(),active=state.isWatched(item.token.address);return <button className={`button watch-button ${active?"active":""}`} aria-pressed={active} onClick={event=>{event.preventDefault();event.stopPropagation();state.toggleWatch(item);notify(active?"Removed from watchlist":"Added to watchlist")}} title={active?"Remove from watchlist":"Add to watchlist"}><Star size={16} fill={active?"currentColor":"none"}/>{compact?null:active?"Watching":"Watch"}</button>}
