"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { Search } from "lucide-react";import { isAddress,getAddress } from "viem";
export function TokenSearch(){const [value,setValue]=useState(""),[error,setError]=useState("");const router=useRouter();function submit(e:React.FormEvent){e.preventDefault();if(!isAddress(value.trim())){setError("Enter a valid EVM contract address.");return}router.push(`/token/${getAddress(value.trim())}`)}return <form onSubmit={submit} style={{maxWidth:720,margin:"32px auto 0"}}><div className="panel" style={{padding:8,display:"flex",gap:8}}><label className="sr-only" htmlFor="address">Token contract address</label><input id="address" value={value} onChange={e=>{setValue(e.target.value);setError("")}} placeholder="Search token contract address" autoComplete="off" style={{flex:1,minWidth:0,background:"transparent",border:0,padding:"13px 14px",fontSize:16,color:"white"}}/><button style={{background:"white",color:"black",border:0,borderRadius:8,padding:"0 18px",fontWeight:700,display:"flex",alignItems:"center",gap:8}}><Search size={17}/>Analyze</button></div>{error&&<p role="alert" className="red" style={{fontSize:13,textAlign:"left"}}>{error}</p>}</form>}
