"use client";import Link from "next/link";import {Clock} from "lucide-react";import {useAppState} from "@/components/providers/AppState";import {TokenAvatar} from "@/components/shared/TokenAvatar";
export function RecentlyViewed(){const s=useAppState();if(!s.ready||!s.recent.length)return null;return <section className="shell section"><div className="section-head"><p className="eyebrow">Your research</p><h2>Recently viewed</h2></div><div className="recent-grid">{s.recent.map(x=><Link className="panel recent-item" href={`/token/${x.address}`} key={x.address}><TokenAvatar address={x.address} symbol={x.symbol} size={34}/><span><strong>{x.symbol}</strong><small>{x.name}</small></span><Clock size={15}/></Link>)}</div></section>}
