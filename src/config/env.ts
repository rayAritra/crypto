import "server-only";
import { z } from "zod";
const schema=z.object({ROBINHOOD_NETWORK:z.enum(["mainnet","testnet"]).default("mainnet"),ROBINHOOD_RPC_URL:z.string().url().default("https://rpc.mainnet.chain.robinhood.com"),ROBINHOOD_CHAIN_ID:z.coerce.number().int().positive().default(4663),ROBINHOOD_CHAIN_NAME:z.string().default("Robinhood Chain"),ROBINHOOD_EXPLORER_URL:z.string().url().default("https://robinhoodchain.blockscout.com"),SUPABASE_URL:z.string().url().optional().or(z.literal("")),SUPABASE_ANON_KEY:z.string().optional(),SUPABASE_SERVICE_ROLE_KEY:z.string().optional(),INDEXER_BASE_URL:z.string().url().default("https://api.geckoterminal.com/api/v2"),INDEXER_CHAIN_ID:z.string().default("robinhood"),INDEXER_API_KEY:z.string().optional(),BLOCKSCOUT_API_URL:z.string().url().default("https://robinhoodchain.blockscout.com/api/v2"),CRON_SECRET:z.string().optional()}).superRefine((v,ctx)=>{const expected=v.ROBINHOOD_NETWORK==="mainnet"?4663:46630;if(v.ROBINHOOD_CHAIN_ID!==expected)ctx.addIssue({code:"custom",path:["ROBINHOOD_CHAIN_ID"],message:`must be ${expected} for ${v.ROBINHOOD_NETWORK}`})});
export type ServerEnv=z.infer<typeof schema>;
export function getEnv():ServerEnv { const r=schema.safeParse(process.env); if(!r.success) throw new Error(`Invalid server configuration: ${r.error.issues.map(i=>`${i.path.join(".")}: ${i.message}`).join(", ")}`); return r.data; }
export function hasDatabase(){return Boolean(process.env.SUPABASE_URL&&process.env.SUPABASE_SERVICE_ROLE_KEY)}
