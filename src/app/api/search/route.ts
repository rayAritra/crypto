import { NextRequest,NextResponse } from "next/server";import { parseAddress } from "@/lib/validation/address";import { rateLimit } from "@/lib/rate-limit";
export async function GET(req:NextRequest){if(!rateLimit(`search:${req.headers.get("x-forwarded-for")??"local"}`))return NextResponse.json({error:{code:"RATE_LIMITED",message:"Too many requests. Try again shortly."}},{status:429});const result=parseAddress(req.nextUrl.searchParams.get("q")??"");return result.success?NextResponse.json({address:result.data,url:`/token/${result.data}`}):NextResponse.json({error:{code:"INVALID_ADDRESS",message:"Enter a valid EVM contract address."}},{status:400})}
