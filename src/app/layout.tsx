import type { Metadata } from "next";import Link from "next/link";import { ScanSearch } from "lucide-react";import "./globals.css";
export const metadata:Metadata={title:{default:"HoodLens — Robinhood Chain Intelligence",template:"%s | HoodLens"},description:"Discover tokens, analyze activity, and understand transparent risk indicators on Robinhood Chain.",openGraph:{title:"HoodLens",description:"Robinhood Chain token intelligence",type:"website"}};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body><header style={{borderBottom:"1px solid var(--border)"}}><nav className="shell" style={{height:68,display:"flex",alignItems:"center",justifyContent:"space-between"}}><Link href="/" style={{display:"flex",gap:10,alignItems:"center",fontWeight:700,fontSize:19}}><ScanSearch size={22}/>HoodLens</Link><span className="muted" style={{fontSize:13}}>Robinhood Chain</span></nav></header>{children}<footer className="shell muted" style={{borderTop:"1px solid var(--border)",padding:"28px 0",fontSize:12}}>HoodLens risk indicators are informational and do not guarantee that a token, contract, or project is safe.</footer></body></html>}
