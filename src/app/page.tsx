import {TokenSearch} from "@/components/home/TokenSearch";import {DiscoveryTabs} from "@/components/home/DiscoveryTabs";import {discovery} from "@/lib/db/queries";import {getEnv} from "@/config/env";import type {RankedToken} from "@/types/token";import {TrendingCards} from "@/components/home/TrendingCards";import {RecentlyViewed} from "@/components/home/RecentlyViewed";import {TokenAvatar} from "@/components/shared/TokenAvatar";import {compactNumber} from "@/lib/utils/format";
export const revalidate=60;
export default async function Home(){let newTokens:RankedToken[]=[],trending:RankedToken[]=[],volume:RankedToken[]=[];try{const explorer=getEnv().ROBINHOOD_EXPLORER_URL;[trending,newTokens,volume]=await Promise.all([discovery("trending",explorer),discovery("new",explorer),discovery("volume",explorer)])}catch{}const all=[...trending,...newTokens,...volume],unique=[...new Map(all.map(x=>[x.token.address,x])).values()];const totalVolume=unique.reduce((n,x)=>n+(x.metrics?.volume24hUsd??0),0),active=unique.filter(x=>(x.metrics?.buys24h??0)+(x.metrics?.sells24h??0)>20).length,newToday=newTokens.length;return <main><section className="hero shell"><div className="hero-copy"><p className="eyebrow">Robinhood Chain intelligence</p><h1>See the market.<br/><span>Read the signal.</span></h1><p>Live contract intelligence for people who want the full picture—price, liquidity, activity, ownership, and transparent risk.</p><TokenSearch/><div className="hero-note"><i/>Live indexed data <span>No synthetic metrics</span></div></div><aside className="hero-board"><header><div><p>Market desk</p><strong>What&apos;s moving</strong></div><span><i/>Live</span></header><div className="hero-board-list">{trending.slice(0,3).map((item,index)=>{const change=item.metrics?.priceChange24h;return <a href={`/token/${item.token.address}`} key={item.token.address}><em>0{index+1}</em><TokenAvatar address={item.token.address} symbol={item.token.symbol} size={38}/><span><strong>{item.token.symbol}</strong><small>{item.token.name}</small></span><div><strong>{compactNumber(item.metrics?.priceUsd,true)}</strong><small className={(change??0)<0?"negative":"positive"}>{change==null?"—":`${change>0?"+":""}${change.toFixed(2)}%`}</small></div></a>})}</div><footer><span>Tracked assets <strong>{unique.length}</strong></span><span>24h volume <strong>{compactNumber(totalVolume,true)}</strong></span></footer></aside></section><section className="pulse"><div className="shell pulse-grid"><div><strong>{unique.length.toLocaleString()}</strong><span>Tokens Tracked</span></div><div><strong>{new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",notation:"compact"}).format(totalVolume)}</strong><span>24H Volume Tracked</span></div><div><strong>{newToday}</strong><span>New Today</span></div><div><strong>{active}</strong><span>High Activity</span></div><small>Across HoodLens-tracked tokens</small></div></section><section className="shell section"><div className="section-head"><p className="eyebrow">Market signals</p><h2>Trending now</h2><p>Tokens ranking highly across tracked volume, activity, liquidity, and momentum.</p></div><TrendingCards tokens={trending}/></section><section className="shell section"><div className="section-head split"><div><p className="eyebrow">Discover</p><h2>Explore tracked tokens</h2></div><span>Real indexed data only</span></div><DiscoveryTabs trending={trending} newTokens={newTokens} volume={volume}/></section><RecentlyViewed/></main>}
