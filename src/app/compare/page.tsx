import {Suspense} from "react";import {CompareClient} from "@/components/compare/CompareClient";export const metadata={title:"Compare Tokens"};export default function Page(){return <main className="shell page"><div className="page-title"><p className="eyebrow">Side-by-side research</p><h1>Compare tokens</h1><p>Compare 2–4 tokens using available market, holder, activity, and risk data.</p></div><Suspense><CompareClient/></Suspense></main>}
